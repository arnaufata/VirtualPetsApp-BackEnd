# application.properties

spring.application.name=VirtualPetsApplication

# Configuració de la base de dades MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/virtual_pets
spring.datasource.username=root
spring.datasource.password=987alr14bt
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuració de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuració del JWT (clau secreta i temps d'expiració)
jwt.secret=una_clau_secreta_secreta_12345
jwt.expiration=86400000    # Temps en mil·lisegons (1 dia)

# Swagger Config
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Additional Swagger UI Config
springdoc.swagger-ui.display-operation-id=true
springdoc.swagger-ui.display-request-duration=true

# Configuració de CORS
cors.allowed-origin=http://localhost:5173